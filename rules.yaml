preview_settings:
  language: python

default:
  enabled: true
  rules:
    - variable naming should be meaningful
    - should provide unit test whenever possible
    - provide code review comment concisely, keep it short
    - use bullet points in the code review comment, with a short description at the beginning

preview_settings:
  language: java

default:
  enabled: true
  rules:
    - function, method, variable name should be meaningful and not ambiguous

## support overriding based on repository
# override:
#  repository:
#    org1/repo1:
#      rules:
#        - imports should follow alphabetic order
#    org2/repo2:
#      rules:
#        - Use descriptive variable names
#        - Use variables even for constants
